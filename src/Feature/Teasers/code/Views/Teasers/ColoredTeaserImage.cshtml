@using Wedia.Feature.Teasers
@using Wedia.Foundation.Alerts
@using Wedia.Foundation.Alerts.Extensions
@using Wedia.Foundation.SitecoreExtensions.Extensions
@model Sitecore.Mvc.Presentation.RenderingModel

@if (!Model?.Item?.DescendsFrom(Templates.HasTeaserContent.ID) ?? true)
{
  @Html.PageEditorError(AlertTexts.InvalidDataSourceTemplate(Templates.HasTeaserContent.ID),
      AlertTexts.InvalidDataSourceTemplateFriendlyMessage,
      Model.Item?.ID,
      Model.Rendering.Item?.ID)
  return;
}

@if (Model.Item.FieldHasValue(Templates.HasTeaserContent.Fields.DesktopImage))
{
  <article class="zigzag__item m-b-64 bg-life-insurance">

    <div class="zigzag__image--light">
      @Html.Sitecore().ImageField(Templates.HasTeaserContent.Fields.DesktopImage, Model.Item, 
        Templates.HasTeaserContent.Dimensions.ColoredImageHeight, 
        Templates.HasTeaserContent.Dimensions.ColoredImageWidth)
    </div>

    <div class="zigzag__info--light">
      @if (Model.Item.FieldHasValue(Templates.HasTeaserHeadline.Fields.Title))
      {
        <h2 class="h4 m-b-16">
          @Html.Sitecore().Field(Templates.HasTeaserHeadline.Fields.Title)
        </h2>
      }
        <div class="zigzag__text--light m-b-32">

          @if (Model.Item.FieldHasValue(Templates.HasTeaserSummary.Fields.Summary))
          {
            @Html.Sitecore().Field(Templates.HasTeaserSummary.Fields.Summary)
          }

        </div>
      </div>

  </article>
}
